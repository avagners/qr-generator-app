{
  "name": "qr-generator-app",
  "version": "1.0.0",
  "description": "Simple client-side QR code generator",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "npm run version && vite build",
    "preview": "vite preview",
    "version": "node -p \"'export const APP_VERSION = \\'' + require('./package.json').version + '\\';'\" > src/version.js"
  },
  "keywords": ["qr-code", "generator", "client-side"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "qrcode": "^1.5.3"
  }
}